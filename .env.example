# Copy this file to `.env` and set your local/private path.
# This file is safe to commit.
# Put `.env` in the project folder while developing, or next to the `.exe` when running a copied build.

# Where Open/Save dialogs should start by default.
# You can use absolute or relative paths.
AK_DIAGRAMS_DEFAULT_DIR=./my-diagrams
